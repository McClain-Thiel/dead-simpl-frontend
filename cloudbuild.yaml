steps:
  # Build the Docker image with branch-specific tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/${_PROJECT_ID}/dead-simpl-frontend/dead-simpl-frontend:${BRANCH_NAME}'
      - '.'

  # Push the tagged image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/${_PROJECT_ID}/dead-simpl-frontend/dead-simpl-frontend:${BRANCH_NAME}'

  # Create a new Cloud Deploy release
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'deploy'
      - 'releases'
      - 'create'
      - 'frontend-release-\${SHORT_SHA}'
      - '--delivery-pipeline=frontend-pipeline'
      - '--region=us-central1'
      - '--source=.'
      - '--images=dead-simpl-frontend=us-central1-docker.pkg.dev/${_PROJECT_ID}/dead-simpl-frontend/dead-simpl-frontend:${BRANCH_NAME}'

# Store build artifacts
images:
  - 'us-central1-docker.pkg.dev/${_PROJECT_ID}/dead-simpl-frontend/dead-simpl-frontend:${BRANCH_NAME}'

# Build timeout
timeout: '1200s'

substitutions:
  _PROJECT_ID: 'halogen-eon-470613-h3'

options:
  logging: CLOUD_LOGGING_ONLY

